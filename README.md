
  # StoryPath: Pitch

  This is a code bundle for StoryPath: Pitch. The original project is available at https://www.figma.com/design/i0wsHnIVvFZmYrJWeybIDB/StoryPath--Pitch.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  